import { PagePlaceholder } from "@/components/app/PagePlaceholder";
import { Building2 } from "lucide-react";

export default function Convenios() {
  return (
    <PagePlaceholder
      title="Convênios"
      description="Gerencie convênios, tabelas de preços e regras de faturamento."
      icon={Building2}
    />
  );
}
